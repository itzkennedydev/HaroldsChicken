import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      'res.cloudinary.com',
      'apply.haroldschickensportsbar.com',
      'live.staticflickr.com'
    ],
  },
};

export default nextConfig;
